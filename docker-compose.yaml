version: '3.3'

services:
    postgres:
        image: "postgres:13" 
        restart: always
        ports:
            - 5433:5432
        env_file:
            - .env
        volumes:
            - database-data:/var/lib/postgresql/data/ # persists data even if container shuts down


volumes:
    database-data:
        driver: local